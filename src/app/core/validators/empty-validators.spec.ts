import {AbstractControl} from "@angular/forms";
import emptyInputValidator from "app/core/validators/empty-validators";

describe('EmptyInputValidator', () => {
  // it('should return true if input is empty', () => {
  //   const control: AbstractControl = {
  //     getRawValue() {
  //       return '';
  //     },
  //   } as AbstractControl;
  //
  //   expect(emptyInputValidator(control)).toStrictEqual({ invalidInput: true });
  // });
});
